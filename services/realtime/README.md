# Realtime service prototype

This standalone Socket.IO service is retained as a backend prototype. It is not yet
connected to the Next.js application and is intentionally excluded from the root
TypeScript build. Install and run it from this directory when integration work begins.

The `examples/` directory contains the original integration sketches.
